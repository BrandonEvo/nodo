fastapi>=0.110.0
uvicorn[standard]>=0.28.0
# Replica la huella TLS/HTTP2 de un navegador real: sin esto Amazon devuelve la
# página anti-bot aunque los headers digan "Chrome".
curl_cffi>=0.7.0
beautifulsoup4>=4.12.0
